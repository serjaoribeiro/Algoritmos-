algoritmo "exercicio 35"
//Sejam a e b os catetos de um triangulo, onde a hipotenusa e obtida pela equacao
var
   a, b:real
inicio
      escreva("Digite os valores do cateto a: ")
      leia(a)
      escreva("Digite o valor do cateto b: ")
      leia(b)
      escreva("o valor premiado pela equaçao para encontrar uma hipotenusa foi:", RaizQ (a* a) + (b * b))
fimalgoritmo
